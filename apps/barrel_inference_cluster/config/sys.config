%% Dev/ct config for the cluster app in isolation: disabled, so `rebar3 shell`
%% and unit suites run as a pure local passthrough without starting mycelium.
[
    {barrel_inference_cluster, [
        {enabled, false}
    ]}
].
